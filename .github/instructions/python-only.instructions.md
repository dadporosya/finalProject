---

## applyTo: "**/*.{py,html,js,css}"

# Python, HTML, JS, and CSS Instructions

* You are permitted to suggest code changes or updates ONLY for files with the following extensions: `.py`, `.html`, `.js`, `.css`
* Do not modify configuration files (e.g., `.yaml`, `.yml`, `.toml`, `.json`, `.ini`) unless explicitly requested in a manual chat prompt.
* When generating backend logic, ensure it is placed within an existing or new **Python (.py)** file.
* Frontend-related changes (UI, styling, browser logic) may be placed in **.html**, **.js**, or **.css** files.
* Do not install virtual environments. Use the existing one located at: `C:\Users\inara\AppData\Local\pypoetry\Cache\virtualenvs\finalproject-QHODVyBn-py3.13`